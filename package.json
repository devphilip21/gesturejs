{
  "private": true,
  "scripts": {
    "postinstall": "[ -n \"$SKIP_POSTINSTALL\" ] || (npx nx build @gesturejs/builder && SKIP_POSTINSTALL=1 pnpm rebuild)"
  },
  "devDependencies": {
    "nx": "^22.2",
    "typescript": "^5.9",
    "vite": "^7.3"
  }
}
